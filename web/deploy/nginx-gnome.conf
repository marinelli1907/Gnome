# Gnome public website — nginx site config for the Hostinger VPS.
# Install:  sudo cp nginx-gnome.conf /etc/nginx/sites-available/gnome
#           sudo ln -s /etc/nginx/sites-available/gnome /etc/nginx/sites-enabled/
#           sudo nginx -t && sudo systemctl reload nginx
# TLS:      sudo certbot --nginx -d gnomefarmersmarket.com -d www.gnomefarmersmarket.com
#           (certbot rewrites this file to add the 443 server + HTTP redirect)

server {
    listen 80;
    listen [::]:80;
    server_name gnomefarmersmarket.com www.gnomefarmersmarket.com;

    location / {
        proxy_pass http://127.0.0.1:3007;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # Next.js immutable build assets — cache hard at the edge.
    location /_next/static/ {
        proxy_pass http://127.0.0.1:3007;
        proxy_set_header Host $host;
        add_header Cache-Control "public, max-age=31536000, immutable";
    }
}
